# langgraph 节点和可控制性
# 节点（node）:图中的基本单元，代表具体的功能和操作 负责完成一项特定任务，接受输入产生输出
# 边(edge): 连接两个节点，表示节点之间的关系和依赖关系(普通边和条件边)
# 图(graph)：由节点和边组成的结构，表示节点之间的关系和依赖关系，代表整个工作流程
# 可控制性：通过节点和边的组合，可以实现对整个工作流程的控制，包括节点的执行顺序、条件判断、循环控制等，从而实现复杂的工作流程管理。
